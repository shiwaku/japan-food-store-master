# 設計: ATP を土台にしたマスター構築

作成: 2026-08-26 ／ 実装: `scripts/build_atp_based_master.py` ／ 出力: `data/food_store_master_atp_based.parquet`
関連 issue: #30（設計）/ #28（投入前の修正）/ #29（調剤特化業態）

> **現行マスター `data/food_store_master.parquet`（102,984件）は変更していない。** 別ファイルとして並行に持つ。

## なぜ主従を逆転させるか

現行は Overture が主・OSM が補完。これを **ATP（All The Places ＋自前クロール、チェーン公式サイト由来）を土台**に
入れ替える。理由は**座標の出所**。

- ATP の座標は**その店自身が公表している位置**であって、第三者データの推定ではない。
- 実際、ATP を第三者データとして当てたことで既存データ側の測地系ズレを2件検出できた
  （マスターのデイリーヤマザキ943件が +358m/−264m、OSM のローソン11,369件が約440m。
  [../sources/検証_ATP現行データ_農水省定義適合.md](../sources/検証_ATP現行データ_農水省定義適合.md) §6）。
- 主要ブランドでは ATP と OSM が 80〜93% で100m以内に一致し、相互に裏が取れている。

ただし **ATP 単独ではマスターにならない**（supermarket 46.6% / fresh_food 0%、
3県の500m圏外率が 67.4% と現行 62.2% より悪化）。あくまで「土台＋補完」。

## 手順

```
python3 scripts/fetch_alltheplaces_jp.py data/atp     # → data/atp_food_stores_japan.parquet
python3 scripts/build_atp_geoparquet.py               # → data/atp_food_stores_japan_geo.parquet
python3 scripts/build_atp_based_master.py [半径m]     # → data/food_store_master_atp_based.parquet
```

1. **土台**: ATP 86,363件
2. **ブランド単位の置き換え**: ATP が spider を持つブランドは ATP を正とし、既存マスター側の同ブランド店を落とす。
   **ブランド名は正規化してから突合する**（`food_store_rules.normalize_brand`）。
   マスター「セブンイレブン」と ATP「セブン-イレブン」が一致しないと同じ店が二重に残る。
3. **空間デデュープ**: 残った既存マスターのうち、同カテゴリの ATP 点が **200m** 以内にあるものを落とす
   （name/brand が欠損していて 2 を素通りしたもの）
4. **補完**: 残りを ATP に足す

fresh_food は ATP がゼロなので、そのまま既存マスター（OSM 由来）が残る。

## 結果

| cat | 現行マスター | 率 | **ATP基準** | 率 | 内訳（ATP / 補完） |
|---|---:|---:|---:|---:|---|
| convenience | 54,792 | 97.2% | 59,826 | 106.2% | 56,910 / 2,916 |
| supermarket | 30,638 | 136.9% | 29,539 | 132.0% | 10,429 / 19,110 |
| drugstore | 7,484 | **42.5%** | 19,659 | **111.6%** | 19,023 / 636 |
| fresh_food | 10,070 | 29.7% | 10,068 | 29.6% | 1 / 10,067 |
| **計** | **102,984** | | **119,092** | | 86,363 / 32,729 |

既存マスター 102,984件のうち、ブランド一致で 61,113件、200m 以内で 9,142件を落とし、32,729件が残った。

### アクセス困難人口での検証（3県88市区町村）

| 指標 | 現行マスター | ATP基準 | ATP を単純に足した版 |
|---|---:|---:|---:|
| 変種A 500m圏外人口（65歳以上） | 685,951（62.2%） | **659,090（59.8%）** | 653,103（59.3%） |
| 変種C 実距離500m | 453,617（41.2%） | 436,473（39.6%） | 430,133（39.0%） |
| 比 農水省÷変種A | 0.420 | **0.437** | 0.441 |
| 比の中央値 / sd / max | 0.364 / 0.108 / 0.768 | 0.381 / 0.115 / **0.793** | 0.372 / 0.118 / 0.878 |
| 全市区町村で 比 ≤ 1 | 満たす | **満たす** | 満たす |

農水省の操作化を壊さずに穴が 26,861人分埋まる。単純に足した版より圏外人口はわずかに多いが、
**重複を落としているぶん max 比が 0.878 → 0.793 と健全**。

## 投入前に直した3点（#28 / #29）

### (a) 座標が系統的にずれる7チェーン

万代・ヨークベニマル・原信ナルス・イズミ・ベルク・オーケーは、**緯度は合っているのに経度だけ
−0.00215度（ほぼ一定・約200m西）**ずれていた。緯度に依存しない一定の経度オフセットなので測地系ではなく、
地図の中心座標をピン位置として拾った類の不具合。住所を100%持っていたので地理院ジオコーダで取り直した。

ヤオコーは `addr:city`「ふじみ野市」と `addr:street_address`「ふじみ野市駒林元町…」で**市名が二重**になり、
ジオコーダが番地まで解決できず市の代表点に潰れていた（204件中146件が同一座標）。

| | 修正前 | 修正後 |
|---|---|---|
| 経度の系統ズレ | −0.00215度 | ほぼ 0 |
| 同ブランド既存店までの距離 中央値 | 165〜285m | **31〜62m** |
| 同一500mメッシュ率 | 48.6〜71.7% | **76.6〜83.5%** |
| ヤオコーの座標潰れ | 146件 | 2件 |

### (b) 調剤特化業態と併設調剤の二重計上

名称に「調剤」を含む drugstore レコード 781件の内訳は、**二重計上 333件**（同一チェーンの非「調剤」店が
50m以内にある。カワチ157・ツルハ113）と**調剤専門店 448件**（スギ薬局調剤241・調剤薬局ツルハドラッグ104）。

判定には罠がある。素朴に「調剤」を含むものを落とすと**ウエルシアを巻き添えにする**。ウエルシアは
併設を注記として店舗名に付けるため（「ウエルシア 江戸川葛西店 (調剤薬局)」＝店舗本体）。
→ **括弧書きを除いた名称に「調剤」が残るもの**だけを調剤特化業態と判定する
（`food_store_rules.is_dispensing_format`）。残った重複は同一チェーン・同一ブランド50m以内で空間的に畳む。

### (c) ブランド表記ゆれ

正規化せずに突合すると同じ店が「純増」に化ける（seven_eleven の純増 3,415件のうち 1,570件＝46%が既存店）。
`normalize_brand` / `normalize_brand_sql` を `food_store_rules.py` に置き、突合前に必ず通す。

## 許可データの投入（2026-08-27）

ATP 基準マスターを**土台**にして、食品営業許可・届出データの純増を足した
`data/food_store_master_atp_permit.parquet`（**139,492件**）を推計用の最新とする。
ATP が原理的に持たない層（fresh_food は ATP でゼロ、地場スーパーは 46.6% で頭打ち）を、
再配布可（CC BY 4.0・政府標準利用規約）のデータで埋める。

| cat | 土台（ATP基準） | ＋許可データ | 計 | 実数統計 | 率 |
|---|---:|---:|---:|---:|---:|
| convenience | 59,826 | 0 | 59,826 | 56,352 | 106.2% |
| supermarket | 29,539 | 3,157 | 32,696 | 22,378 | 146.1%※ |
| fresh_food | 10,068 | **17,243** | **27,311** | 33,960 | **80.4%** |
| drugstore | 19,659 | 0 | 19,659 | 17,622 | 111.6% |
| **計** | **119,092** | **20,400** | **139,492** | | |

※ supermarket の分母は 561+581（23,401）に取り直すべき（issue #33）。
`src_cat` で分解すると本体は不足側にある。→ [検証_supermarket分解と分母の確定.md](検証_supermarket分解と分母の確定.md)

### アクセス困難人口での検証（3県88市区町村）

| 指標 | 現行マスター | ATP基準 | **ATP基準＋許可データ** |
|---|---:|---:|---:|
| 変種A 500m圏外人口（65歳以上） | 685,951（62.2%） | 659,090（59.8%） | **631,711（57.3%）** |
| 変種C 実距離500m | 453,617（41.2%） | 436,473（39.6%） | 409,885（37.2%） |
| 比 農水省÷変種A | 0.420 | 0.437 | **0.456** |
| 比の max | 0.768 | 0.793 | 0.793 |
| 全市区町村で 比 ≤ 1 | 満たす | 満たす | **満たす** |
| 農水省公表値との相関 r | 0.448 | 0.448 | **0.462** |

**件数を足せば圏外率は必ず下がるので、圏外率の改善だけでは判定材料にならない。**
相関 r が 0.448 → 0.462 に上がったことが「穴を埋めた」ことの根拠

> **⚠️ この根拠は47県では成立しない**（2026-08-27・issue #46）。1,740市区町村で測り直すと
> Phase1 の r 0.305 に対し ATP基準＋許可は **0.271 と下がり**、比>1 も 44→102件に倍増する。
> さらに全国の r 自体が都市-地方のコントラストで稼いでいて、**地方部内では r=0.158**（Phase1 0.177）。
> **純増が都市部に多いソースは、地方の穴を1つも埋めなくても r を動かせる。**
> 投入可否は**47県・地方部限定**で測り直すこと。→ [japan-food-access-analysis#3](https://github.com/shiwaku/japan-food-access-analysis/pull/3)
（[../sources/検証_許可データ_生鮮3業種_補完効果.md](../sources/検証_許可データ_生鮮3業種_補完効果.md)）。

市区町村別は `検証_アクセス困難人口_市区町村別_ATP許可基準.csv` / カテゴリ感度は
`検証_アクセス困難人口_カテゴリ感度_ATP許可基準.csv`。感度は
S1 supermarket のみ 81.9% → S2 +convenience 64.3% → S3 +drugstore 62.1% → S4 全カテゴリ **57.3%** で、
**fresh_food の寄与が −4.7pt** と最大になった（補完前は −3.3pt）。

### 再現

```
python3 scripts/build_atp_based_master.py           # ① 土台（突合の基準）
FOOD_MASTER=data/food_store_master_atp_based.parquet \
    OUT_PARQUET=data/permit_fresh_food_candidates_atp.parquet \
    OUT_CSV=docs/sources/検証_許可データ_生鮮3業種_都道府県別_ATP基準.csv \
    python3 scripts/extract_permit_fresh_food.py    # ② 候補（生鮮）
FOOD_MASTER=data/food_store_master_atp_based.parquet \
    OUT_PARQUET=data/permit_supermarket_candidates_atp.parquet \
    OUT_CSV=docs/sources/検証_許可データ_総合スーパー_都道府県別_ATP基準.csv \
    python3 scripts/extract_permit_supermarkets.py  # ② 候補（⑪）
python3 scripts/merge_permit_gapfill.py             # ③ 投入
```

**候補は「どのマスターに対して純増か」で変わる**ので、②と③には同じ土台を渡すこと。

## ライセンス

出力の `redistributable` 列で行単位に判別できる。

| source_class | 件数 | 再配布 |
|---|---:|---|
| `atp_official_cc0`（ATP 公式ラン14本） | 39,358 | 可（CC-0） |
| `self_crawl`（自前クロール38本） | 47,005 | **不可**（各社規約） |
| `overture_osm` | 32,729 | 可 |
| `permit`（食品営業許可・届出） | 20,400 | 可（CC BY 4.0 / 政府標準利用規約） |

**推計・分析に使うことは制約なし**（取得も解析目的の複製も禁じられていない）。禁じられているのは
店舗データそのものの再配布なので、公開物に出すときだけ `redistributable` で絞る。
詳細は [../sources/調査_自前クロールソースの利用規約と再配布可否.md](../sources/調査_自前クロールソースの利用規約と再配布可否.md)。

## 残課題

| 課題 | 現状 | issue |
|---|---|---|
| supermarket が実数の 132% | **分母の取り方が誤っていた**（2026-08-27 に確定）。`src_cat` で分解すると Overture supermarket 由来はセンサス561+581 の 79.0%＝**不足**で、grocery_store 由来は589系（分母が手元に無い）。→ [検証_supermarket分解と分母の確定.md](検証_supermarket分解と分母の確定.md) | #33 |
| convenience が 106.2% | 置き換え後も残る偽陽性の精査が要る | #34 |
| fresh_food 29.6% | ATP は原理的に埋められない。許可データでの補完 | #26 |
| 自前クロール28チェーンの規約未調査 | 2026-08-25 クロール分 | #35 |
| 現行マスターのデイリーヤマザキ測地系ズレ | ATP 基準では解消するが、現行マスターは誤ったまま | #36 |
| OSM のローソン440mズレ | viewer の `osm_food.pmtiles` に影響 | #37 |
| 検証が3県88市区町村のみ | 47県への拡張。ATP 基準は都市部が偏って埋まるので都市の多い県で挙動が変わりうる | #38 |
| 保守 | 52本のクローラを維持することになる。公式サイト改修で壊れる | — |
